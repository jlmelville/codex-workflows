interface:
  display_name: "R Architecture Audit"
  short_description: "Extend architecture reviews with R package evidence"
  default_prompt: "Use $r-architecture-audit with the shared architecture-audit contract to review this R package or diff and trace consumers before cleanup."
