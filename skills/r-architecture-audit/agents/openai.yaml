interface:
  display_name: "R Architecture Audit"
  short_description: "Map, compare, and trace R package architecture"
  default_prompt: "Use $r-architecture-audit to map or compare this R package and trace structural consumers before cleanup."
